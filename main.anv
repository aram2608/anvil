if 1 == 1 {
 1 + 1.0;
} else {
 1 + 2;
}

foo := 4;
bar := foo + 4.0;

hello := "hello";

world := hello + "world";

if hello == "hello" {
    @printf("yes");
}

// TODO:
// Consider changing the syntax so that the last 
// expression in a block is bare
// { foo };
add := fn(a, b) { a + b; };

num := add(4, 5);

@printf(num);
